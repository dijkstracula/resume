name: Render resume

on:
  push:
    branches: [master]
    paths:
      - data.yaml
      - render.py
      - style.css
      - resume.typ
      - components.typ
      - data.typ

  # Allow triggering manually from the Actions tab.
  workflow_dispatch:

jobs:
  render:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # needed to push the regenerated index.html

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - uses: typst-community/setup-typst@v4

      - name: Install dependencies
        run: pip install pyyaml

      - name: Render index.html
        run: python render.py

      - name: Render resume.pdf
        run: typst compile resume.typ resume.pdf

      - name: Upload resume.pdf artifact
        uses: actions/upload-artifact@v4
        with:
          name: resume-pdf
          path: resume.pdf

      - name: Commit and push if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html
          # Only commit when index.html actually changed.
          if git diff --staged --quiet; then
            echo "index.html unchanged — nothing to commit."
          else
            git commit -m "chore: regenerate index.html"
            git push
          fi
